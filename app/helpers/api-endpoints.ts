export const baseUrl = `http://api.localhost:3000/v1`;

export const createAccountUrl = `${baseUrl}/accounts/signup`;
export const authAccountUrl = `${baseUrl}/accounts/login`;
export const resetPasswordUrl = `${baseUrl}/accounts/reset-password`;
export const getSessionProfile = `${baseUrl}/accounts/me`;
export const reports = `${baseUrl}/reports`;
export const notifications = `${baseUrl}/notifications`;
